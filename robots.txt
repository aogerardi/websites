User-agent: *
Allow: /

# Demos stay out of search and off the public site. The exception below is now
# a redirect: Plainview went live on its own domain, and the old demo URL is
# kept so links already shared in email/Teams/iMessage still resolve. Unfurlers
# must be able to FETCH it to follow the redirect and read the og: tags — a
# Disallow here is what silently kills that.
Allow: /demos/plainview-oil/
Disallow: /demos/
Disallow: /command/

Sitemap: https://piedmondo.com/sitemap.xml
