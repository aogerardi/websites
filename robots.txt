User-agent: *
Allow: /

# Demos stay out of search and off the public site. The one exception below is
# a live pitch we need link-preview cards for (Teams/Slack/iMessage unfurlers
# have to be able to FETCH the page to read its og: tags — a Disallow here is
# what silently kills the preview). The page still carries meta noindex, so it
# can be fetched for a preview but will not be listed in search results.
Allow: /demos/plainview-oil/
Disallow: /demos/
Disallow: /command/

Sitemap: https://piedmondo.com/sitemap.xml
