# Local credentials for the Command Center (Apps Script /exec URL + team key).
# Format: line 1 = EXEC_URL, line 2 = KEY. Never commit.
pipeline/.command-key

.DS_Store

# Taken-down demos: code kept locally, never re-published
demos-archive/
